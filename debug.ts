import { bitgetService } from "./server/api/bitget/bitgetService.ts"; (async () => { try { console.log("Fetching tickers..."); const tickers = await bitgetService.getAllTickers(); console.log("Response type:", typeof tickers); console.log("Is array:", Array.isArray(tickers)); console.log("Response length:", tickers.length); console.log("Sample ticker formats:"); for (let i = 0; i < Math.min(3, tickers.length); i++) { console.log(`[${i}]:`, JSON.stringify(tickers[i])); } // Check exact formatting console.log("Looking for BTC pairs:"); const btcPairs = tickers.filter(ticker => ticker.symbol && ticker.symbol.startsWith("BTC")); console.log(`Found ${btcPairs.length} BTC pairs. First 3:`); for (let i = 0; i < Math.min(3, btcPairs.length); i++) { console.log(`[${i}]:`, JSON.stringify(btcPairs[i])); } } catch (error) { console.error("Error:", error); }})();
