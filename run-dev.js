/**
 * Development Server Start Script
 * 
 * This script is a wrapper around the main start.cjs file, designed to be
 * called by npm run dev. It provides compatibility with the existing workflow.
 */

// Import the main start script by requiring it
require('./start.cjs');